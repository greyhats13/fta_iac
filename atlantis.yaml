version: 3
projects:
  - dir: deployment/cloud
    apply_requirements: ["mergeable"]
    autoplan:
      when_modified: ["*.tf*"]
  - dir: deployment/service/dev/fta_users
    apply_requirements: ["mergeable"]
    autoplan:
      when_modified: ["*.tf*"]
      enabled: true
  - dir: deployment/service/prd/fta_users
    apply_requirements: ["mergeable"]
    autoplan:
      when_modified: ["*.tf*"]
      enabled: true